image: "hseeberger/scala-sbt"

# disable gitlab cache (the directory /cache is mapped to a physical directory on the server)
#cache:
#  key: "$CI_BUILD_REF_NAME" # contains either the branch or the tag, so it's caching per branch
#  untracked: true
#  paths:
#    - "/cache/.ivy"
#    - "/cache/.boot"
#    - "/cache/.sbtboot"
#    - "/cache/target"

variables:
  SBT_OPTS: "-Dsbt.global.base=/sbt-cache/.sbtboot -Dsbt.boot.directory=/sbt-cache/.boot -Dsbt.ivy.home=/sbt-cache/.ivy"

stages:
  - allinone

allinone:
  stage: allinone
  script:
    - sbt clean coverage test coverageReport doc publish
    - cp target/scala-2.11/scoverage-report/index.html public/coverage.html
    - cp -r target/scala-2.11/api public/api
  artifacts:
    paths:
      - public
